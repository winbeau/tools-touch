[project]
name = "tools-touch-python"
version = "0.1.0"
description = "Tools Touch Python workspace"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["baoyan-cli", "collector-host"]
