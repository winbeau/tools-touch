# Tools Touch 项目接手约定

## 开始前

- 新对话接手整体架构实施时，先读 [Luna 接手入口](docs/design/START-HERE.md) 和 [实施状态](docs/design/IMPLEMENTATION-STATE.md)，再读当前步骤需要的文档。
- 用户当前会话的指令优先。设计文档中的“本轮仅写文档”描述设计会话，不是对后续编码的禁止。
- 当前包含已实现的 WPF／Pi／Gmail／SQLite 应用；扩展时保留既有用户资料与业务不变量。

## 工程约定

- 主体为 C# WPF；应用自有 Node 包用 pnpm，Python 项目及脚本用 uv。现有 npm 锁文件的迁移按 P00 执行，不在未验证时直接删除。
- `pi/` 是上游参考源码，不纳入应用 workspace，不修改其 provider 认证和 Agent Loop。
- C# 独占业务数据库写入；Pi 不获得 Gmail 凭据、发送工具或任意 shell／文件写权限。
- 往年窗口只生成明确标记的今年预测，不能据此判定今年实际结束。
- 学校分表是 UI／Excel 视图；数据库使用统一关系模型。自定义表字段、关联与全量导出是首版范围。
- 用户确认后才发送具体邮件；发送 Unknown 不自动重试。表格编辑、导入、恢复均不得触发邮件发送。

## 执行与续接

- 按 [文件迁移与工作包](docs/design/03-implementation-map.md) 完成一个可验证工作包后，再进入下一个；不要一次重写全部层。
- 工作包中的新文件路径是目标，先检查实际仓库再创建；不得覆盖其他未提交修改，也不得清理未跟踪的 baoyan-cli／设计文件。
- 现有 C# 测试是可执行程序，用 `dotnet run --project ...`；仅 `dotnet test` 返回成功不能算测试已执行。
- 运行当前变更需要的检查，记录命令、退出码与证据；模拟通过、真实来源、Windows 原生和真实账号验收分别标记。
- 每个工作包结束更新 `docs/design/IMPLEMENTATION-STATE.md`。未实际完成的步骤不标完成；环境缺口只阻塞依赖该环境的验收。
- 不因为一个工作包完成就认定用户要求的整个实施目标完成。依照当前用户授权继续后续可执行工作。
- 除非用户要求，不自行提交 Git、推送或发布版本。
- 用户已确定：发布使用正式 GitHub Release，不使用 prerelease；打包、安装验收与发布统一交给 `.github/workflows/release.yml`，通过版本标签触发。不要把本地候选包手工上传作为正式交付，已公开版本不得覆盖。
